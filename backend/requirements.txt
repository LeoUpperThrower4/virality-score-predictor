# ── Core server ──
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── TRIBE v2 (Meta brain-encoding model) ──
tribev2[plotting] @ git+https://github.com/facebookresearch/tribev2.git

# ── Brain region mapping ──
# nilearn>=0.11 supports numpy 2.x, which TRIBE v2 pins to 2.2.6.
nilearn>=0.11.0

# ── Utilities ──
requests>=2.31.0

# TRIBE v2 internally runs whisperx via `uvx` for speech-to-text.
uv>=0.4.0
